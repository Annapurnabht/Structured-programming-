#include <stdio.h>
#include <math.h>

int main() {
    printf("Square root = %.2f\n", sqrt(25));
    printf("Power = %.2f\n", pow(2, 3));
    printf("Absolute value = %d\n", abs(-10));

    return 0;
}